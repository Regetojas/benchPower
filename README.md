# benchPower
Python scripts for controlling bench power from the command line

To set up in a new virtual environment:
```shell-session
% python -m venv venv
% pip install -r requirements.txt
```
Edit the #! line at the top of each script to show the path to the venv python exec.